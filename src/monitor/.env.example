# Keycloak Authentication
# Use either client credentials OR username/password
KEYCLOAK_URL=https://id.augur.world
KEYCLOAK_REALM=augur
KEYCLOAK_CLIENT_ID=your-client-id
KEYCLOAK_CLIENT_SECRET=your-client-secret
# OR use username/password instead:
# KEYCLOAK_USERNAME=your-username
# KEYCLOAK_PASSWORD=your-password

# API Configuration
API_URL=http://localhost:8000
API_PATH=/api

# Monitoring Configuration
# Check interval in seconds (default: 300 = 5 minutes)
CHECK_INTERVAL=300
# Email cooldown in seconds (default: 43200 = 12 hours)
EMAIL_COOLDOWN=43200

# Thresholds (percentage)
# Alert when usage exceeds these values
CPU_CRITICAL=90.0
MEMORY_CRITICAL=90.0
DISK_CRITICAL=90.0

# SMTP Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=monitor@example.com

# Email Recipients (comma-separated list)
EMAIL_RECIPIENTS=admin@example.com,ops@example.com

# Optional: Log file path (default: monitor.log)
# LOG_FILE=monitor.log
